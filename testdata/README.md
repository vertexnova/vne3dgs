# Test fixture for the PLY reader (Task 03).
#
# three_gaussians.ply — binary little-endian, 3 vertices, SH degree 3.
# Generated by a short Python packer (struct `<f`) with the same property
# order as `writeGaussianPly`:
#
#   x y z nx ny nz f_dc_0..2 f_rest_0..44 opacity scale_0..2 rot_0..3
#
# Chosen stored values:
# - Gaussian 0: scale_0 = log(2), opacity logit = 0 (-> 0.5),
#   rot = (2, 0, 0, 0) file order (-> identity), f_rest_k = k,
#   f_dc = (0.1, -0.2, 0.3), position (1, 2, 3).
# - Gaussian 1: rot = (0, 1, 0, 0) file order (-> Quatf(1, 0, 0, 0)),
#   unit scales, zero DC / rest.
# - Gaussian 2: scale (3, 2, 1), opacity logit = 2, identity rotation,
#   f_rest_k = k, position (-1, 0.5, 2).
#
# Regenerate with the Python block in the agent notes for Task 03, or by
# writing an equivalent cloud through `writeGaussianPly` and copying the file.
